from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='We want to test mlops end to end pipeline',
    author='Vipin',
    license='MIT',
)
